services:
  go-update-header:
    build: .
    ports:
      - "8080:8080" 
    networks:
      - go-update-header-network
    restart: unless-stopped

networks:
  go-update-header-network:
    driver: bridge
